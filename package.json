{
  "name": "layer-eraser",
  "version": "1.0.0",
  "description": "图层擦除工具",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
